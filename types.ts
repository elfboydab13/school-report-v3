export interface StudentData {
  studentName: string;
  powerschoolData: string;
  ixlData: string;
  behaviorData: string;
  focusArea: string;
}

export interface Student {
  id: string;
  name:string;
}
